import { ArrowUpRight, FileText } from "lucide-react";
import { catalogConfig, siteConfig } from "@/content/site";

export function CatalogCards() {
  return <div className="grid gap-6 lg:grid-cols-2">{catalogConfig.map((catalog) => <article key={catalog.title} className="overflow-hidden border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="grid min-h-[280px] grid-cols-[.7fr_1.3fr]"><div className="bg-[hsl(var(--primary))] p-4"><img src={`${import.meta.env.BASE_URL}${catalog.cover}`} alt={`Portada del ${catalog.title} de ${catalog.brand}`} className="h-full w-full object-cover" loading="lazy" width="320" height="420" /></div><div className="flex flex-col p-6 md:p-8"><p className="eyebrow text-[hsl(var(--destructive))]">{catalog.brand}</p><h2 className="mt-3 font-display text-3xl leading-none text-[hsl(var(--primary))]">{catalog.title}</h2><p className="mt-4 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">{catalog.description}</p><div className="mt-5 flex flex-wrap gap-2 text-xs text-[hsl(var(--muted-foreground))]">{catalog.categories.map((item) => <span key={item} className="border border-[hsl(var(--border))] px-2 py-1">{item}</span>)}</div><p className="mt-auto flex items-center gap-2 pt-6 text-xs font-bold uppercase tracking-[.12em] text-[hsl(var(--primary))]"><FileText className="h-4 w-4 text-[hsl(var(--destructive))]" /> PDF oficial · {catalog.pages}</p></div></div><div className="flex flex-wrap items-center gap-4 border-t border-[hsl(var(--border))] px-6 py-5 md:px-8"><a href={catalog.pdfUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 bg-[hsl(var(--primary))] px-4 py-3 text-sm font-bold text-[hsl(var(--primary-foreground))]">Ver catálogo PDF <ArrowUpRight className="h-4 w-4" /></a><a href={`${siteConfig.whatsappLink}?text=${encodeURIComponent(catalog.message)}`} target="_blank" rel="noreferrer" className="text-sm font-bold text-[hsl(var(--primary))] underline decoration-[hsl(var(--accent))] underline-offset-4">Consultar con IMAG</a><p className="w-full text-xs text-[hsl(var(--muted-foreground))]">El PDF se abre en una pestaña nueva y desde allí puede descargarse.</p></div></article>)}</div>;
}