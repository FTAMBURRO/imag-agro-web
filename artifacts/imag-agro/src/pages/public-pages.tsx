import { FormEvent, useEffect, useMemo, useState } from 'react';
import { Link, useLocation } from 'wouter';
import { ArrowDown, ArrowRight, ArrowUpRight, ClipboardList, MessageCircle, MoveRight, Sprout, Wheat } from 'lucide-react';
import { articles, getArticle } from '@/content/articles';
import { brands, siteConfig } from '@/content/site';
import { getService, services } from '@/content/services';
import { Breadcrumb, PageFrame } from '@/components/site-shell';

function FieldPanel({ label = 'El campo, en contexto', dark = false }: { label?: string; dark?: boolean }) {
  return <div className={`field-art min-h-[360px] ${dark ? 'bg-[hsl(var(--primary))]' : ''}`}><span className="absolute left-5 top-5 z-10 vertical-label eyebrow text-[hsl(var(--primary-foreground))]/80">{label}</span><span className="absolute bottom-6 right-6 z-10 max-w-[155px] text-right font-display text-2xl leading-tight text-[hsl(var(--primary-foreground))]">La decisión empieza antes.</span><span className="crop-lines absolute inset-x-0 bottom-0 h-1/2 opacity-60" /></div>;
}

function SectionIntro({ eyebrow, title, children, action }: { eyebrow: string; title: string; children?: React.ReactNode; action?: React.ReactNode }) {
  return <div className="mb-10 flex flex-col gap-5 md:mb-14 md:flex-row md:items-end md:justify-between"><div><p className="eyebrow text-[hsl(var(--destructive))]">{eyebrow}</p><h2 className="mt-4 max-w-2xl text-balance font-display text-4xl leading-[1.02] text-[hsl(var(--primary))] md:text-6xl">{title}</h2>{children && <div className="mt-5 max-w-xl text-[1.02rem] leading-relaxed text-[hsl(var(--muted-foreground))]">{children}</div>}</div>{action}</div>;
}

function usePageMeta(title: string, description: string) {
  useEffect(() => {
    document.title = `${title} | IMAG AGRO SAS`;
    const descriptionTag = document.querySelector('meta[name="description"]');
    descriptionTag?.setAttribute('content', description);
  }, [description, title]);
}

export function HomePage() {
  usePageMeta('Soluciones agropecuarias que producen resultados', 'Semillas, genética bovina, hacienda, forrajes, insumos y gestión documental de granos para productores y empresas agropecuarias.');
  const [finder, setFinder] = useState('');
  const recommended = useMemo(() => services.filter((service) => {
    if (!finder) return true;
    const terms: Record<string, string[]> = { producir: ['semillas', 'insumos', 'forrajes'], vender: ['hacienda', 'granos-y-back-office'], ordenar: ['granos-y-back-office', 'campos'], invertir: ['campos', 'genetica-bovina'], ganado: ['genetica-bovina', 'hacienda', 'forrajes'] };
    return terms[finder]?.includes(service.slug) ?? true;
  }).slice(0, 4), [finder]);

  return <PageFrame>
    <section className="overflow-hidden bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]">
      <div className="container-wide grid min-h-[650px] items-center gap-12 py-20 md:grid-cols-[1.05fr_.95fr] md:py-28">
        <div className="reveal">
          <p className="eyebrow text-[hsl(var(--accent))]">Decisiones con los pies en la tierra</p>
          <h1 className="mt-7 max-w-2xl font-display text-6xl leading-[.94] md:text-8xl">Cada decisión del campo, <em className="text-[hsl(var(--accent))]">mejor acompañada.</em></h1>
          <p className="mt-8 max-w-lg text-lg leading-relaxed text-[hsl(var(--primary-foreground))]/70">En IMAG AGRO acercamos información, productos y criterio para que productores y empresas puedan decidir con más claridad.</p>
          <div className="mt-10 flex flex-wrap gap-5"><Link href="/soluciones" className="group flex items-center gap-3 bg-[hsl(var(--accent))] px-5 py-3.5 font-bold text-[hsl(var(--foreground))] transition-transform hover:-translate-y-0.5" data-testid="link-hero-soluciones">Conocé nuestras soluciones <ArrowUpRight className="h-4 w-4" /></Link><Link href="/nosotros" className="flex items-center gap-2 border-b border-[hsl(var(--primary-foreground))]/40 py-3 text-sm text-[hsl(var(--primary-foreground))]/80 hover:border-[hsl(var(--accent))] hover:text-[hsl(var(--accent))]" data-testid="link-hero-nosotros">Cómo trabajamos <ArrowRight className="h-4 w-4" /></Link></div>
        </div>
        <div className="reveal reveal-delay-2 relative"><FieldPanel label="Argentina productiva" /><div className="absolute -bottom-8 -left-4 max-w-[220px] border border-[hsl(var(--accent))]/50 bg-[hsl(var(--primary))] p-5 md:-left-10"><p className="eyebrow text-[hsl(var(--accent))]">Desde el contexto</p><p className="mt-3 text-sm leading-relaxed text-[hsl(var(--primary-foreground))]/75">No miramos una decisión aislada. Miramos el sistema que tiene que sostenerla.</p></div></div>
      </div>
      <div className="container-wide flex items-center gap-3 pb-8 text-[.7rem] tracking-[.16em] text-[hsl(var(--primary-foreground))]/45"><ArrowDown className="h-4 w-4 text-[hsl(var(--accent))]" /> DESLIZÁ PARA CONOCER IMAG AGRO</div>
    </section>

    <section className="container-wide py-24 md:py-32">
      <SectionIntro eyebrow="Una mirada completa" title="El campo no se divide en categorías. Las decisiones tampoco." action={<Link href="/soluciones" className="group flex items-center gap-2 border-b border-[hsl(var(--primary))] pb-2 text-sm font-bold text-[hsl(var(--primary))]" data-testid="link-home-all-solutions">Ver todas las soluciones <MoveRight className="h-4 w-4 transition-transform group-hover:translate-x-1" /></Link>}>
        Trabajamos donde una buena lectura puede ordenar lo que viene: producción, comercialización y operación.
      </SectionIntro>
      <div className="grid border-y border-[hsl(var(--border))] md:grid-cols-3">
        {[{ icon: Sprout, title: 'Producir mejor', copy: 'Semillas, insumos, forrajes y genética para pensar el sistema antes del producto.' }, { icon: Wheat, title: 'Comercializar con criterio', copy: 'Hacienda, campos y granos para encontrar una alternativa clara en cada momento.' }, { icon: ClipboardList, title: 'Operar sin perder el hilo', copy: 'Documentación, seguimiento y una conversación cercana cuando el negocio lo necesita.' }].map(({ icon: Icon, title, copy }, index) => <div key={title} className={`py-8 md:px-8 ${index !== 0 ? 'border-t md:border-l md:border-t-0' : ''}`}><Icon className="h-7 w-7 text-[hsl(var(--destructive))]" strokeWidth={1.4} /><h3 className="mt-7 font-display text-3xl text-[hsl(var(--primary))]">{title}</h3><p className="mt-4 max-w-xs text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">{copy}</p></div>)}
      </div>
    </section>

    <section className="bg-[hsl(var(--secondary))] py-24 md:py-32">
      <div className="container-wide">
        <SectionIntro eyebrow="Un punto de partida" title="¿Qué necesitás resolver?" action={<p className="hidden max-w-[190px] text-right text-xs leading-relaxed text-[hsl(var(--muted-foreground))] md:block">Elegí una situación y te mostramos por dónde empezar la conversación.</p>} />
        <div className="flex flex-wrap gap-2 border-b border-[hsl(var(--border))] pb-7">
          {[['producir', 'Quiero producir'], ['ganado', 'Estoy pensando en ganado'], ['vender', 'Necesito vender'], ['ordenar', 'Quiero ordenar'], ['invertir', 'Estoy buscando invertir']].map(([value, label]) => <button key={value} type="button" onClick={() => setFinder(finder === value ? '' : value)} className={`border px-4 py-3 text-sm transition-colors ${finder === value ? 'border-[hsl(var(--primary))] bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border-[hsl(var(--border))] bg-[hsl(var(--background))] hover:border-[hsl(var(--primary))]'}`} data-testid={`button-finder-${value}`}>{label}</button>)}
        </div>
        <div className="mt-8 grid gap-x-8 gap-y-0 md:grid-cols-2">{recommended.map((service) => <Link key={service.slug} href={`/soluciones/${service.slug}`} className="group flex items-start justify-between border-b border-[hsl(var(--border))] py-5" data-testid={`link-recommended-${service.slug}`}><div><span className="eyebrow text-[hsl(var(--destructive))]">{service.number}</span><h3 className="mt-2 font-display text-2xl text-[hsl(var(--primary))]">{service.name}</h3><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">{service.short}</p></div><ArrowUpRight className="mt-1 h-5 w-5 text-[hsl(var(--primary))] transition-transform group-hover:-translate-y-1 group-hover:translate-x-1" /></Link>)}</div>
      </div>
    </section>

    <section id="como-trabajamos" className="container-wide grid gap-14 py-24 md:grid-cols-[.85fr_1.15fr] md:py-32">
      <div><p className="eyebrow text-[hsl(var(--destructive))]">Así trabajamos</p><h2 className="mt-4 font-display text-5xl leading-[1.02] text-[hsl(var(--primary))] md:text-6xl">Primero escuchamos. Después, pensamos juntos.</h2><Link href="/nosotros" className="mt-8 inline-flex items-center gap-2 text-sm font-bold text-[hsl(var(--primary))] underline decoration-[hsl(var(--accent))] decoration-2 underline-offset-8" data-testid="link-home-method">Conocé nuestra forma de trabajar <ArrowRight className="h-4 w-4" /></Link></div>
      <div className="grid gap-0 border-t border-[hsl(var(--border))]">{[['01', 'Entender el momento', 'Nos metemos en la situación concreta: el lote, el rodeo, la entrega, el papel que falta.'], ['02', 'Poner la información en común', 'Traducimos datos y alternativas para conversar lo importante, sin vueltas.'], ['03', 'Elegir un próximo paso', 'Una buena decisión también necesita saber qué hacer después y cuándo volver a mirar.']].map(([number, title, copy]) => <div key={number} className="grid grid-cols-[54px_1fr] gap-5 border-b border-[hsl(var(--border))] py-7"><span className="font-mono text-xs text-[hsl(var(--destructive))]">{number}</span><div><h3 className="font-display text-2xl text-[hsl(var(--primary))]">{title}</h3><p className="mt-2 max-w-md text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">{copy}</p></div></div>)}</div>
    </section>

    <section id="marcas" className="bg-[hsl(var(--primary))] py-24 text-[hsl(var(--primary-foreground))] md:py-28">
      <div className="container-wide grid gap-12 md:grid-cols-[1fr_1fr] md:items-center"><div><p className="eyebrow text-[hsl(var(--accent))]">Marcas y alianzas</p><h2 className="mt-5 max-w-xl font-display text-5xl leading-[1.04] md:text-6xl">Una red para ampliar la conversación.</h2><p className="mt-6 max-w-md leading-relaxed text-[hsl(var(--primary-foreground))]/65">Trabajamos con un portfolio de marcas y alianzas que conocemos y podemos poner en contexto. Sin promesas de exclusividad: con criterio para cada necesidad.</p></div><div className="grid grid-cols-1 border-y border-[hsl(var(--sidebar-border))]">{brands.map((brand, index) => <div key={brand} className="flex items-center justify-between border-b border-[hsl(var(--sidebar-border))] py-5 last:border-b-0"><span className="font-display text-3xl">{brand}</span><span className="eyebrow text-[hsl(var(--accent))]">0{index + 1}</span></div>)}</div></div>
    </section>

    <section className="container-wide py-24 md:py-32"><SectionIntro eyebrow="Para seguir pensando" title="Información para productores" action={<Link href="/informacion" className="group flex items-center gap-2 border-b border-[hsl(var(--primary))] pb-2 text-sm font-bold text-[hsl(var(--primary))]" data-testid="link-home-information">Ver toda la información <MoveRight className="h-4 w-4 transition-transform group-hover:translate-x-1" /></Link>} /><div className="grid gap-8 md:grid-cols-[1.25fr_.75fr]">{articles.slice(0, 3).map((article, index) => <Link key={article.slug} href={`/informacion/${article.slug}`} className={`${index === 0 ? 'md:row-span-2' : ''} group border-t border-[hsl(var(--border))] pt-6`} data-testid={`link-home-article-${article.slug}`}><p className="eyebrow text-[hsl(var(--destructive))]">{article.category}</p><h3 className={`${index === 0 ? 'text-4xl md:text-5xl' : 'text-2xl'} mt-4 font-display leading-[1.05] text-[hsl(var(--primary))] transition-colors group-hover:text-[hsl(var(--destructive))]`}>{article.title}</h3><p className="mt-4 max-w-md text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">{article.excerpt}</p><span className="mt-8 inline-flex items-center gap-2 text-xs font-bold text-[hsl(var(--primary))]">{article.readingTime} <ArrowUpRight className="h-4 w-4" /></span></Link>)}</div></section>

    <section className="bg-[hsl(var(--accent))] py-20"><div className="container-wide flex flex-col gap-8 md:flex-row md:items-end md:justify-between"><div><p className="eyebrow text-[hsl(var(--foreground))]/65">Cuando quieras, lo conversamos</p><h2 className="mt-4 max-w-2xl font-display text-5xl leading-[.98] text-[hsl(var(--foreground))] md:text-7xl">Soluciones que producen resultados.</h2></div><Link href="/contacto" className="group flex w-fit items-center gap-3 border border-[hsl(var(--foreground))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--foreground))] transition-colors hover:bg-[hsl(var(--foreground))] hover:text-[hsl(var(--accent))]" data-testid="link-home-contacto">Hablemos de tu próximo paso <ArrowUpRight className="h-4 w-4 transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" /></Link></div></section>
  </PageFrame>;
}

export function SolutionsPage() {
  usePageMeta('Soluciones para el campo real', 'Conocé las áreas de IMAG AGRO: semillas, genética bovina, insumos, forrajes, materias primas, hacienda, campos y granos.');
  const [filter, setFilter] = useState('Todas');
  const categories = ['Todas', 'Producción', 'Ganadería', 'Comercial', 'Gestión'];
  const categoryFor = (slug: string) => slug.includes('genetica') || slug === 'hacienda' || slug === 'forrajes' ? 'Ganadería' : slug.includes('granos') || slug === 'campos' ? 'Comercial' : 'Producción';
  const visible = services.filter((s) => filter === 'Todas' || categoryFor(s.slug) === filter);
  return <PageFrame><div className="container-wide py-16 md:py-24"><Breadcrumb current="Soluciones" /><div className="grid gap-12 md:grid-cols-[.8fr_1.2fr]"><div><p className="eyebrow text-[hsl(var(--destructive))]">Una conversación, varias puertas</p><h1 className="mt-5 font-display text-6xl leading-[.95] text-[hsl(var(--primary))] md:text-8xl">Soluciones para el campo real.</h1></div><div className="md:pt-12"><p className="max-w-lg text-xl leading-relaxed text-[hsl(var(--muted-foreground))]">No todo problema necesita el mismo camino. Conocé las áreas en las que podemos acompañarte y encontrá la que mejor describe tu momento.</p></div></div><div className="mt-20 flex flex-wrap gap-2 border-b border-[hsl(var(--border))] pb-4">{categories.map((category) => <button type="button" key={category} onClick={() => setFilter(category)} className={`px-4 py-2 text-sm ${filter === category ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border border-[hsl(var(--border))] hover:border-[hsl(var(--primary))]'}`} data-testid={`button-filter-${category.toLowerCase()}`}>{category}</button>)}</div><div className="mt-2 grid md:grid-cols-2">{visible.map((service) => <Link href={`/soluciones/${service.slug}`} key={service.slug} className="group border-b border-[hsl(var(--border))] py-8 md:px-7 md:py-10 md:first:pl-0 md:nth-[2n+1]:pl-0 md:nth-[2n]:border-l" data-testid={`link-service-${service.slug}`}><div className="flex items-start justify-between"><span className="eyebrow text-[hsl(var(--destructive))]">{service.number}</span><ArrowUpRight className="h-5 w-5 text-[hsl(var(--primary))] transition-transform group-hover:-translate-y-1 group-hover:translate-x-1" /></div><h2 className="mt-8 font-display text-4xl text-[hsl(var(--primary))]">{service.name}</h2><p className="mt-3 max-w-md leading-relaxed text-[hsl(var(--muted-foreground))]">{service.description}</p><span className="mt-7 inline-block text-sm font-bold underline decoration-[hsl(var(--accent))] decoration-2 underline-offset-4">Ver cómo acompañamos</span></Link>)}</div></div></PageFrame>;
}

export function ServicePage({ slug }: { slug?: string }) {
  const service = getService(slug);
  usePageMeta(service ? service.name : 'Soluciones', service?.description ?? 'Soluciones agropecuarias para productores y empresas.');
  if (!service) return <NotFoundPage />;
  return <PageFrame><section className="bg-[hsl(var(--primary))] py-16 text-[hsl(var(--primary-foreground))] md:py-24"><div className="container-wide"><Breadcrumb current={service.name} /><div className="grid gap-12 md:grid-cols-[.95fr_1.05fr] md:items-end"><div><p className="eyebrow text-[hsl(var(--accent))]">Solución {service.number}</p><h1 className="mt-5 max-w-2xl font-display text-6xl leading-[.95] md:text-8xl">{service.name}</h1></div><p className="max-w-lg text-xl leading-relaxed text-[hsl(var(--primary-foreground))]/70">{service.description}</p></div></div></section><section className="container-wide grid gap-16 py-20 md:grid-cols-[.9fr_1.1fr] md:py-28"><FieldPanel label={service.name} /><div><p className="eyebrow text-[hsl(var(--destructive))]">En contexto</p><h2 className="mt-5 max-w-xl font-display text-5xl leading-[1.02] text-[hsl(var(--primary))]">La mejor respuesta depende de la pregunta correcta.</h2><p className="mt-7 max-w-xl text-lg leading-relaxed text-[hsl(var(--muted-foreground))]">{service.detail}</p><Link href="/contacto" className="mt-9 inline-flex items-center gap-3 bg-[hsl(var(--primary))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="link-service-contacto">Conversar sobre {service.name.toLowerCase()} <ArrowUpRight className="h-4 w-4" /></Link></div></section><section className="bg-[hsl(var(--secondary))] py-20"><div className="container-wide"><p className="eyebrow text-[hsl(var(--destructive))]">Para empezar la charla</p><h2 className="mt-4 font-display text-4xl text-[hsl(var(--primary))]">Tres preguntas que nos ayudan a entender</h2><div className="mt-10 grid gap-0 border-t border-[hsl(var(--border))] md:grid-cols-3">{service.questions.map((question, index) => <div className="border-b border-[hsl(var(--border))] py-7 md:border-r md:px-7 md:first:pl-0 md:last:border-r-0" key={question}><span className="font-mono text-xs text-[hsl(var(--destructive))]">0{index + 1}</span><p className="mt-5 font-display text-2xl leading-tight text-[hsl(var(--primary))]">{question}</p></div>)}</div></div></section><section className="container-wide py-20"><div className="flex flex-col gap-6 border-t border-[hsl(var(--border))] pt-8 md:flex-row md:items-center md:justify-between"><p className="max-w-xl font-display text-3xl text-[hsl(var(--primary))]">¿Querés contarnos un poco más de tu situación?</p><Link href="/contacto" className="flex w-fit items-center gap-2 border-b border-[hsl(var(--primary))] pb-2 text-sm font-bold" data-testid="link-service-bottom-contacto">Abrir contacto <ArrowRight className="h-4 w-4" /></Link></div></section></PageFrame>;
}

export function InformationPage() {
  usePageMeta('Información para productores', 'Ideas y preguntas para mirar producción, negocio y operación con un poco más de claridad.');
  const [category, setCategory] = useState('Todas');
  const categories = ['Todas', 'Agricultura', 'Ganadería', 'Mercados', 'Clima', 'Gestión'];
  const visible = articles.filter((article) => category === 'Todas' || article.category === category);
  return <PageFrame><div className="container-wide py-16 md:py-24"><Breadcrumb current="Información" /><div className="max-w-3xl"><p className="eyebrow text-[hsl(var(--destructive))]">Información para productores</p><h1 className="mt-5 font-display text-6xl leading-[.95] text-[hsl(var(--primary))] md:text-8xl">Pensar el campo también es parte del trabajo.</h1><p className="mt-7 max-w-xl text-xl leading-relaxed text-[hsl(var(--muted-foreground))]">Ideas y preguntas para mirar producción, negocio y operación con un poco más de claridad.</p></div><div className="mt-20 flex flex-wrap gap-2 border-b border-[hsl(var(--border))] pb-4">{categories.map((item) => <button type="button" key={item} onClick={() => setCategory(item)} className={`px-4 py-2 text-sm ${category === item ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border border-[hsl(var(--border))] hover:border-[hsl(var(--primary))]'}`} data-testid={`button-article-filter-${item.toLowerCase()}`}>{item}</button>)}</div><div className="mt-8 grid gap-x-9 md:grid-cols-2">{visible.map((article, index) => <Link href={`/informacion/${article.slug}`} className={`group border-b border-[hsl(var(--border))] py-8 ${index === 0 ? 'md:col-span-2 md:py-12' : ''}`} key={article.slug} data-testid={`link-article-${article.slug}`}><div className="flex items-start justify-between"><p className="eyebrow text-[hsl(var(--destructive))]">{article.category}</p>{article.status && <span className="text-xs text-[hsl(var(--destructive))]">Revisión técnica pendiente</span>}</div><h2 className={`${index === 0 ? 'max-w-3xl text-5xl md:text-6xl' : 'text-3xl'} mt-4 font-display leading-[1.03] text-[hsl(var(--primary))] group-hover:text-[hsl(var(--destructive))]`}>{article.title}</h2><p className="mt-4 max-w-xl leading-relaxed text-[hsl(var(--muted-foreground))]">{article.excerpt}</p><span className="mt-6 inline-flex items-center gap-2 text-xs font-bold text-[hsl(var(--primary))]">{article.readingTime} <ArrowUpRight className="h-4 w-4" /></span></Link>)}</div></div></PageFrame>;
}

export function ArticlePage({ slug }: { slug?: string }) {
  const article = getArticle(slug);
  usePageMeta(article ? article.title : 'Información', article?.excerpt ?? 'Información para productores agropecuarios.');
  if (!article) return <NotFoundPage />;
  return <PageFrame><article className="container-wide py-16 md:py-24"><Breadcrumb current={article.title} /><div className="max-w-4xl"><p className="eyebrow text-[hsl(var(--destructive))]">{article.category}</p><h1 className="mt-5 font-display text-5xl leading-[1] text-[hsl(var(--primary))] md:text-7xl">{article.title}</h1><p className="mt-7 max-w-2xl text-xl leading-relaxed text-[hsl(var(--muted-foreground))]">{article.excerpt}</p><div className="mt-8 flex flex-wrap items-center gap-4 text-xs text-[hsl(var(--muted-foreground))]"><span>{article.readingTime}</span>{article.status && <span className="border border-[hsl(var(--destructive))] px-3 py-1.5 text-[hsl(var(--destructive))]">Revisión técnica pendiente</span>}</div></div><div className="mt-14 grid gap-14 md:grid-cols-[1fr_280px]"><div className="max-w-2xl border-t border-[hsl(var(--border))] pt-10">{article.body.map((paragraph) => <p key={paragraph} className="mb-7 font-display text-2xl leading-relaxed text-[hsl(var(--primary))]">{paragraph}</p>)}{article.status && <div className="mt-12 border-l-2 border-[hsl(var(--destructive))] bg-[hsl(var(--secondary))] p-5 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]"><strong className="text-[hsl(var(--primary))]">Aviso editorial.</strong> Este contenido está en revisión técnica pendiente. La información es general y no reemplaza el asesoramiento profesional aplicado a cada caso.</div>}</div><aside className="h-fit border-t border-[hsl(var(--border))] pt-6"><p className="eyebrow text-[hsl(var(--destructive))]">Seguí leyendo</p><div className="mt-5 space-y-5">{articles.filter((item) => item.slug !== article.slug).slice(0, 3).map((item) => <Link href={`/informacion/${item.slug}`} key={item.slug} className="group block" data-testid={`link-related-${item.slug}`}><p className="font-display text-xl leading-tight text-[hsl(var(--primary))] group-hover:text-[hsl(var(--destructive))]">{item.title}</p><span className="mt-2 inline-flex text-xs text-[hsl(var(--muted-foreground))]">{item.category}</span></Link>)}</div></aside></div></article></PageFrame>;
}

export function AboutPage() {
  usePageMeta('Nosotros', 'IMAG AGRO es una empresa argentina de soluciones agropecuarias que conecta la realidad productiva con el próximo paso.');
  return <PageFrame><section className="bg-[hsl(var(--primary))] py-16 text-[hsl(var(--primary-foreground))] md:py-24"><div className="container-wide"><Breadcrumb current="Nosotros" /><div className="grid gap-12 md:grid-cols-[1fr_1fr] md:items-end"><h1 className="font-display text-6xl leading-[.95] md:text-8xl">Cerca del campo. Claros al decidir.</h1><p className="max-w-md text-xl leading-relaxed text-[hsl(var(--primary-foreground))]/70">IMAG AGRO es una empresa argentina de soluciones agropecuarias. Construimos conversaciones que conectan la realidad productiva con el próximo paso.</p></div></div></section><section className="container-wide grid gap-14 py-24 md:grid-cols-[1fr_1fr] md:py-32"><FieldPanel label="Una forma de estar" /><div><p className="eyebrow text-[hsl(var(--destructive))]">Lo que nos mueve</p><h2 className="mt-5 font-display text-5xl leading-[1.02] text-[hsl(var(--primary))]">La cercanía no es una pose. Es una forma de trabajar.</h2><p className="mt-7 leading-relaxed text-[hsl(var(--muted-foreground))]">Preferimos conocer el contexto antes de ofrecer una respuesta. Preguntar antes de asumir. Volver sobre lo conversado cuando el campo cambia. Así se construye una relación útil: con presencia, criterio y respeto por cada realidad.</p><p className="mt-5 leading-relaxed text-[hsl(var(--muted-foreground))]">No presentamos historias de clientes ni resultados que no podamos documentar. Presentamos una manera de hacer las cosas.</p></div></section><section className="bg-[hsl(var(--secondary))] py-20"><div className="container-wide"><p className="eyebrow text-[hsl(var(--destructive))]">Principios de trabajo</p><div className="mt-8 grid gap-0 border-t border-[hsl(var(--border))] md:grid-cols-3">{[['Contexto', 'La misma herramienta puede servir o no, según dónde y cuándo.'], ['Claridad', 'Lo importante tiene que poder explicarse sin rodeos.'], ['Continuidad', 'Una decisión mejora cuando alguien sigue ahí después.']].map(([title, copy], index) => <div key={title} className="border-b border-[hsl(var(--border))] py-8 md:border-r md:px-8 md:first:pl-0 md:last:border-r-0"><span className="font-mono text-xs text-[hsl(var(--destructive))]">0{index + 1}</span><h2 className="mt-6 font-display text-3xl text-[hsl(var(--primary))]">{title}</h2><p className="mt-3 max-w-xs text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">{copy}</p></div>)}</div></div></section><section className="container-wide py-24"><div className="flex flex-col gap-8 border-t border-[hsl(var(--border))] pt-9 md:flex-row md:items-end md:justify-between"><div><p className="eyebrow text-[hsl(var(--destructive))]">El próximo paso</p><h2 className="mt-4 max-w-xl font-display text-5xl leading-[1] text-[hsl(var(--primary))]">Si hay una pregunta, ya hay por dónde empezar.</h2></div><Link href="/contacto" className="flex w-fit items-center gap-2 border-b border-[hsl(var(--primary))] pb-2 text-sm font-bold" data-testid="link-about-contacto">Abrir una conversación <ArrowRight className="h-4 w-4" /></Link></div></section></PageFrame>;
}

export function ContactPage() {
  usePageMeta('Contacto', 'Contanos qué necesitás resolver y abrí una conversación directa con IMAG AGRO por WhatsApp.');
  const [form, setForm] = useState({ name: '', subject: '', message: '' });
  const [started, setStarted] = useState(false);
  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const message = `Hola, soy ${form.name}. Me gustaría consultar por ${form.subject}.\n\n${form.message}`;
    const target = `${siteConfig.whatsappLink}?text=${encodeURIComponent(message)}`;
    setStarted(true);
    window.open(target, '_blank', 'noopener,noreferrer');
  };
  return <PageFrame><div className="container-wide py-16 md:py-24"><Breadcrumb current="Contacto" /><div className="grid gap-16 md:grid-cols-[.9fr_1.1fr]"><div><p className="eyebrow text-[hsl(var(--destructive))]">Hablemos</p><h1 className="mt-5 font-display text-6xl leading-[.95] text-[hsl(var(--primary))] md:text-8xl">Contanos qué estás mirando.</h1><p className="mt-7 max-w-md text-lg leading-relaxed text-[hsl(var(--muted-foreground))]">No hace falta llegar con la respuesta. Con un poco de contexto, podemos encontrar una mejor pregunta y el camino para seguir.</p><a href={siteConfig.whatsappLink} target="_blank" rel="noreferrer" className="mt-9 inline-flex items-center gap-3 bg-[hsl(var(--primary))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="link-contacto-whatsapp"><MessageCircle className="h-4 w-4" /> Escribir por WhatsApp</a></div>{siteConfig.whatsappNumber ? <div className="border-t border-[hsl(var(--border))] pt-7"><p className="eyebrow text-[hsl(var(--destructive))]">Antes de abrir WhatsApp</p><h2 className="mt-4 font-display text-3xl text-[hsl(var(--primary))]">Dejanos el contexto de tu consulta</h2><form onSubmit={handleSubmit} className="mt-8 space-y-6"><label className="block"><span className="text-sm font-semibold text-[hsl(var(--primary))]">Tu nombre o empresa</span><input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="mt-2 w-full border-0 border-b border-[hsl(var(--border))] bg-transparent px-0 py-3 text-base outline-none focus:border-[hsl(var(--primary))]" placeholder="¿Cómo te llamamos?" data-testid="input-contact-name" /></label><label className="block"><span className="text-sm font-semibold text-[hsl(var(--primary))]">¿Qué necesitás resolver?</span><select required value={form.subject} onChange={(event) => setForm({ ...form, subject: event.target.value })} className="mt-2 w-full border-0 border-b border-[hsl(var(--border))] bg-transparent px-0 py-3 text-base outline-none focus:border-[hsl(var(--primary))]" data-testid="select-contact-subject"><option value="">Elegí una opción</option>{services.map((service) => <option value={service.name} key={service.slug}>{service.name}</option>)}<option value="Otra consulta">Otra consulta</option></select></label><label className="block"><span className="text-sm font-semibold text-[hsl(var(--primary))]">Un poco más de contexto</span><textarea required rows={4} value={form.message} onChange={(event) => setForm({ ...form, message: event.target.value })} className="mt-2 w-full resize-y border border-[hsl(var(--border))] bg-transparent p-3 text-base outline-none focus:border-[hsl(var(--primary))]" placeholder="Contanos lo que creas importante..." data-testid="textarea-contact-message" /></label><button type="submit" className="flex items-center gap-3 bg-[hsl(var(--accent))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--foreground))]" data-testid="button-contact-submit">Abrir WhatsApp con mi consulta <ArrowUpRight className="h-4 w-4" /></button>{started && <p className="border-l-2 border-[hsl(var(--accent))] bg-[hsl(var(--secondary))] p-4 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]" role="status">La consulta quedó en este dispositivo. WhatsApp se abrió para que puedas continuar la conversación.</p>}</form></div> : <div className="border-t border-[hsl(var(--border))] pt-7"><p className="eyebrow text-[hsl(var(--destructive))]">Canal directo</p><h2 className="mt-4 font-display text-3xl text-[hsl(var(--primary))]">Escribinos por WhatsApp y contanos qué necesitás.</h2><p className="mt-5 max-w-md text-base leading-relaxed text-[hsl(var(--muted-foreground))]">Por ahora usamos un enlace directo de WhatsApp. Cuando haya un número habilitado, podés activar desde la configuración el formulario con mensaje prearmado.</p><a href={siteConfig.whatsappLink} target="_blank" rel="noreferrer" className="mt-8 inline-flex items-center gap-3 border-b border-[hsl(var(--primary))] pb-2 text-sm font-bold text-[hsl(var(--primary))]" data-testid="link-contacto-whatsapp-secondary">Abrir el canal directo <ArrowUpRight className="h-4 w-4" /></a></div>}</div></div></PageFrame>;
}

export function NotFoundPage() {
  usePageMeta('Página no encontrada', 'No encontramos la dirección que buscabas. Volvé al inicio de IMAG AGRO.');
  const [, setLocation] = useLocation();
  return <PageFrame><div className="container-wide flex min-h-[65vh] flex-col justify-center py-20"><p className="eyebrow text-[hsl(var(--destructive))]">404 · Camino no encontrado</p><h1 className="mt-5 max-w-2xl font-display text-6xl leading-[.95] text-[hsl(var(--primary))] md:text-8xl">Esta página se fue al lote.</h1><p className="mt-7 max-w-md text-lg leading-relaxed text-[hsl(var(--muted-foreground))]">No encontramos la dirección que buscabas. Podemos volver a empezar desde el campo.</p><button type="button" onClick={() => setLocation('/')} className="mt-9 flex w-fit items-center gap-3 bg-[hsl(var(--primary))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-404-home">Volver al inicio <ArrowRight className="h-4 w-4" /></button></div></PageFrame>;
}